# apply only to this group; empty for all groups (default: all)
group=

# interactive prompt every this many messages; 0 for never, 1 for always, and >1 for random chance (default: 5)
prompt_chance=5

# time budget in seconds to answer a prompt; 0 for unlimited (default: 60)
timeout=60
